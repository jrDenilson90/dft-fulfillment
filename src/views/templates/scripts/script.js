document.addEventListener('DOMContentLoaded', function() {
    // const elemento = document.querySelector('.parallax');
    // const parallaxImage = document.querySelector('.parallaxImage');

    // const observer = new IntersectionObserver((entries) => {
    //     entries.forEach(entry => {
    //         if (entry.isIntersecting) {
    //             // Inicia o efeito parallax
    //             window.addEventListener('scroll', parallaxEffect);
    //             observer.unobserve(entry.target); // Para parar de observar após a primeira visibilidade
    //             console.log("AQUI");
    //         }
    //     });
    // });

    // observer.observe(elemento);

    // function parallaxEffect() {
    //     const scrollPosition = window.scrollY;
    //     parallaxImage.style.transform = 'translateY(' + scrollPosition * 0.4 + 'px)';
    // }
});